# research/ — vecu (ACU)

Per-block lab-notebook space: design rationale, dead ends, benchmarks, and exploration notes that
give the next human/agent the *why* (not just the RTL). Rides along in the block's mirror.

Chip-wide APA precision-policy research (the RL project that produced this datapath) is at the repo
root: `research/apa-precision-policy/`. Record experiments here as *result · n · artifact · script*.
